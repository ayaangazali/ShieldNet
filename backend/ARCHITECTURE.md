# ShieldNet Backend Architecture

## 🏗️ System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│                        FRONTEND (React + Vite)                       │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐           │
│  │ Invoice  │  │ Treasury │  │ Threat   │  │Analytics │           │
│  │ Upload   │  │Dashboard │  │Analytics │  │Dashboard │           │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘           │
└───────┼─────────────┼─────────────┼─────────────┼──────────────────┘
        │             │             │             │
        │ HTTP/REST API (JSON)      │             │
        ▼             ▼             ▼             ▼
┌─────────────────────────────────────────────────────────────────────┐
│                     FASTAPI APPLICATION (main.py)                    │
│  ┌───────────────────────────────────────────────────────────┐     │
│  │                    MIDDLEWARE LAYER                        │     │
│  │  • CORS Handler  • Request Logger  • Error Handler        │     │
│  └───────────────────────────────────────────────────────────┘     │
│                                                                      │
│  ┌──────────────────────────────────────────────────────────┐      │
│  │                    API ROUTERS                            │      │
│  │  ┌───────────┐  ┌──────────┐  ┌──────────┐              │      │
│  │  │ Invoices  │  │ Treasury │  │ Threats  │              │      │
│  │  │  Router   │  │  Router  │  │  Router  │              │      │
│  │  └─────┬─────┘  └────┬─────┘  └────┬─────┘              │      │
│  │  ┌─────────┐  ┌──────────┐  ┌──────────┐                │      │
│  │  │Analytics│  │ Mandates │  │ Vendors  │                │      │
│  │  │ Router  │  │  Router  │  │  Router  │                │      │
│  │  └─────┬───┘  └────┬─────┘  └────┬─────┘                │      │
│  └────────┼───────────┼─────────────┼──────────────────────┘      │
│           │           │             │                              │
│           ▼           ▼             ▼                              │
│  ┌──────────────────────────────────────────────────────────┐     │
│  │                    BUSINESS LOGIC SERVICES                │     │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐   │     │
│  │  │  PDF Parser  │  │ Verification │  │    Fraud     │   │     │
│  │  │   Service    │  │   Service    │  │  Detection   │   │     │
│  │  │              │  │              │  │   Service    │   │     │
│  │  │ • Extract    │  │ • PO Check   │  │ • Scoring    │   │     │
│  │  │ • Parse      │  │ • Contract   │  │ • Decision   │   │     │
│  │  │ • Template   │  │ • Vendor     │  │ • Risk Calc  │   │     │
│  │  └──────────────┘  └──────────────┘  └──────────────┘   │     │
│  │  ┌──────────────┐                                         │     │
│  │  │   Treasury   │                                         │     │
│  │  │   Service    │                                         │     │
│  │  │              │                                         │     │
│  │  │ • Pay()      │                                         │     │
│  │  │ • Mandates   │                                         │     │
│  │  │ • Web3       │                                         │     │
│  │  └──────────────┘                                         │     │
│  └──────────────────────────────────────────────────────────┘     │
│                             │                                       │
└─────────────────────────────┼───────────────────────────────────────┘
                              │
                              ▼
          ┌───────────────────────────────────────┐
          │         DATABASE LAYER                │
          │  ┌─────────────────────────────────┐  │
          │  │  SQLAlchemy ORM + AsyncPG       │  │
          │  └────────────┬────────────────────┘  │
          │               │                       │
          │               ▼                       │
          │  ┌─────────────────────────────────┐  │
          │  │   PostgreSQL Database           │  │
          │  │                                 │  │
          │  │  • invoices                     │  │
          │  │  • vendors                      │  │
          │  │  • purchase_orders              │  │
          │  │  • contracts                    │  │
          │  │  • threat_reports               │  │
          │  │  • transactions                 │  │
          │  │  • mandates                     │  │
          │  │  • treasury_wallets             │  │
          │  │  • audit_logs                   │  │
          │  └─────────────────────────────────┘  │
          └───────────────────────────────────────┘

          ┌───────────────────────────────────────┐
          │      EXTERNAL INTEGRATIONS            │
          │  ┌─────────────────────────────────┐  │
          │  │  Locus Wallet (Web3)            │  │
          │  │  • USDC Contract                │  │
          │  │  • Payment Execution            │  │
          │  │  • Balance Queries              │  │
          │  └─────────────────────────────────┘  │
          │  ┌─────────────────────────────────┐  │
          │  │  ShieldNet Network API          │  │
          │  │  • Threat Intelligence          │  │
          │  │  • Shared Fraud Data            │  │
          │  └─────────────────────────────────┘  │
          └───────────────────────────────────────┘
```

## 🔄 Data Flow Diagrams

### Invoice Processing Flow

```
┌─────────┐
│ Upload  │
│ Invoice │
└────┬────┘
     │
     ▼
┌─────────────────┐
│  Parse PDF/JSON │◄─── PDF Parser Service
└────┬────────────┘
     │
     ▼
┌─────────────────┐
│ Extract Fields  │
│ • Vendor        │
│ • Amount        │
│ • PO Number     │
│ • Line Items    │
│ • Template Hash │
└────┬────────────┘
     │
     ▼
┌─────────────────┐
│ Local           │◄─── Verification Service
│ Verification    │      • PO Match
│                 │      • Contract Check
│                 │      • Vendor Trust
│                 │      • Duplicate Check
└────┬────────────┘
     │
     ▼
┌─────────────────┐
│ Network Fraud   │◄─── Fraud Detection Service
│ Check           │      • Template Threat
│                 │      • Wallet Threat
│                 │      • ShieldNet Query
└────┬────────────┘
     │
     ▼
┌─────────────────┐
│ Calculate       │
│ Scores          │
│ • Confidence    │
│ • Fraud         │
└────┬────────────┘
     │
     ▼
┌─────────────────┐
│ Make Decision   │
│                 │
│ ┌─────────────┐ │
│ │   APPROVE   │ │──► confidence ≥ 0.85
│ └─────────────┘ │    fraud ≤ 0.15
│ ┌─────────────┐ │
│ │    HOLD     │ │──► Medium risk
│ └─────────────┘ │
│ ┌─────────────┐ │
│ │    BLOCK    │ │──► fraud ≥ 0.7
│ └─────────────┘ │    OR network threat
└────┬────────────┘
     │
     ▼
┌─────────────────┐
│ Return Analysis │
│ Results to UI   │
└─────────────────┘
```

### Payment Execution Flow

```
┌──────────────┐
│ Approved     │
│ Invoice      │
└──────┬───────┘
       │
       ▼
┌──────────────────┐
│ Check Mandates   │◄─── Treasury Service
│ • Amount limit   │
│ • Score thresh   │
│ • Vendor rules   │
└──────┬───────────┘
       │
       │ Pass?
       ▼
┌──────────────────┐
│ Check Balance    │◄─── Web3 Query
│ (Blockchain)     │
└──────┬───────────┘
       │
       │ Sufficient?
       ▼
┌──────────────────┐
│ Build Transaction│
│ • From: Locus    │
│ • To: Vendor     │
│ • Amount: USDC   │
└──────┬───────────┘
       │
       ▼
┌──────────────────┐
│ Sign Transaction │◄─── Private Key
└──────┬───────────┘
       │
       ▼
┌──────────────────┐
│ Send to          │
│ Blockchain       │
└──────┬───────────┘
       │
       ▼
┌──────────────────┐
│ Record           │
│ Transaction      │
│ in Database      │
└──────┬───────────┘
       │
       ▼
┌──────────────────┐
│ Update Invoice   │
│ Status: PAID     │
└──────────────────┘
```

### Threat Detection Flow

```
┌──────────────┐
│ Blocked      │
│ Invoice      │
└──────┬───────┘
       │
       ▼
┌──────────────────────┐
│ Generate Fingerprints│
│ • Vendor hash        │
│ • Wallet hash        │
│ • Template hash      │
└──────┬───────────────┘
       │
       ▼
┌──────────────────────┐
│ Create Threat Report │
│ • Type               │
│ • Severity           │
│ • Indicators         │
│ • Amount saved       │
└──────┬───────────────┘
       │
       ▼
┌──────────────────────┐
│ Store in Local       │
│ Threat Database      │
└──────┬───────────────┘
       │
       ▼
┌──────────────────────┐
│ Anonymize Data       │
└──────┬───────────────┘
       │
       ▼
┌──────────────────────┐
│ Share with           │
│ ShieldNet Network    │
│ (Optional)           │
└──────┬───────────────┘
       │
       ▼
┌──────────────────────┐
│ Other Companies      │
│ Query & Benefit      │
└──────────────────────┘
```

## 🎯 Component Interactions

### Invoice Upload → Decision
```
Frontend → POST /api/invoices/upload
    ↓
InvoicesRouter.upload_invoice()
    ↓
PDFParserService.parse_pdf() ────► Extract structured data
    ↓
VerificationService.verify_invoice() ─► Check PO, contracts, vendors
    ↓
FraudDetectionService.calculate_scores() ─► Calculate risk scores
    ↓
FraudDetectionService._make_decision() ───► Approve/Hold/Block
    ↓
Database: Save invoice with scores
    ↓
Return: InvoiceAnalysisResponse
```

### Treasury Payment
```
Frontend → POST /api/treasury/pay/{id}
    ↓
TreasuryRouter.pay_invoice()
    ↓
TreasuryService.pay_invoice()
    ↓
TreasuryService._check_mandates() ────► Policy evaluation
    ↓
TreasuryService._check_balance() ─────► Blockchain query
    ↓
TreasuryService._execute_payment() ───► Web3 transaction
    ↓
Database: Record transaction
    ↓
Return: Payment result with tx_hash
```

## 📊 Database Schema Relationships

```
┌──────────┐       ┌──────────┐
│ Vendors  │───1:N─│ Invoices │
└──────────┘       └────┬─────┘
                        │ 1:1
                   ┌────▼────────┐
                   │Transactions │
                   └─────────────┘
                        │ 1:N
                   ┌────▼────────────┐
                   │ Threat Reports  │
                   └─────────────────┘

┌──────────┐       ┌────────────────┐
│ Vendors  │───1:N─│ Purchase Orders│
└──────────┘       └────────────────┘

┌──────────┐       ┌──────────┐
│ Vendors  │───1:N─│Contracts │
└──────────┘       └──────────┘

┌──────────┐
│ Mandates │  (No relations - policy rules)
└──────────┘

┌────────────────┐
│Treasury Wallets│  (Singleton-ish)
└────────────────┘

┌────────────┐
│ Audit Logs │  (Logging only)
└────────────┘
```

## 🔐 Security Layers

```
┌────────────────────────────────────────┐
│          FRONTEND LAYER                │
│  • Input validation                    │
│  • File type checking                  │
└──────────┬─────────────────────────────┘
           │
           ▼
┌────────────────────────────────────────┐
│      MIDDLEWARE LAYER                  │
│  • CORS validation                     │
│  • Request logging                     │
│  • Error handling                      │
└──────────┬─────────────────────────────┘
           │
           ▼
┌────────────────────────────────────────┐
│      ROUTER LAYER                      │
│  • Authentication (JWT ready)          │
│  • Authorization (roles)               │
│  • Rate limiting (ready)               │
└──────────┬─────────────────────────────┘
           │
           ▼
┌────────────────────────────────────────┐
│      VALIDATION LAYER                  │
│  • Pydantic schemas                    │
│  • Type checking                       │
│  • Business rules                      │
└──────────┬─────────────────────────────┘
           │
           ▼
┌────────────────────────────────────────┐
│      SERVICE LAYER                     │
│  • Mandate policy checks               │
│  • Fraud detection                     │
│  • Verification rules                  │
└──────────┬─────────────────────────────┘
           │
           ▼
┌────────────────────────────────────────┐
│      DATABASE LAYER                    │
│  • SQL injection prevention (ORM)      │
│  • Transaction isolation               │
│  • Audit logging                       │
└────────────────────────────────────────┘
```

## 🚀 Scalability Considerations

- **Async Operations**: All I/O is non-blocking
- **Connection Pooling**: SQLAlchemy async pool
- **Horizontal Scaling**: Stateless API design
- **Caching Ready**: Redis integration prepared
- **Background Jobs**: Celery integration ready
- **Load Balancing**: Container-ready with Docker
- **Database Indexing**: Proper indexes on queries
- **API Versioning**: Ready for `/api/v2/`

## 📈 Performance Optimizations

- **Async Database**: Non-blocking queries
- **Bulk Operations**: Ready for batch processing
- **Query Optimization**: Select only needed fields
- **Lazy Loading**: Relationships loaded on demand
- **Caching Strategy**: Ready for Redis caching
- **File Streaming**: Large file upload support
- **Pagination**: All list endpoints support pagination
